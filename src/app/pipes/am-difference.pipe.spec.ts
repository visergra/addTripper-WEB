import { AmDifferencePipe } from './am-difference.pipe';

describe('AmDifferencePipe', () => {
  it('create an instance', () => {
    const pipe = new AmDifferencePipe();
    expect(pipe).toBeTruthy();
  });
});
