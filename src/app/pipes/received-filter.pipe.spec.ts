import { ReceivedFilterPipe } from './received-filter.pipe';

describe('ReceivedFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ReceivedFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
